# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# Hugging Face access token (required for gated models, e.g. Meta-Llama-3-8B-Instruct).
# Generate one at: https://huggingface.co/settings/tokens
HUGGING_FACE_TOKEN=